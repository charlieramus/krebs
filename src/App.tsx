/**
 * Placeholder shell. There is no interface in V1.
 *
 * The kernel is headless and is exercised through `npm run sim` and the test
 * suite. The visual contract in DESIGN.md gets applied in V3, after the
 * kinetics have answered whether they feel like anything. See src/ui/README.md.
 */
export function App() {
  return (
    <main className="p-8 font-mono text-sm">
      <p>krebs, V1 kernel. No interface yet. Run `npm run sim`.</p>
    </main>
  );
}
